# URL backend без слэша в конце (для production-сборки)
REACT_APP_API_URL=https://flower-api.onrender.com

# Тот же VAPID_PUBLIC_KEY, что на backend (или подтянется с /api/push/vapid-public-key)
REACT_APP_VAPID_PUBLIC_KEY=
